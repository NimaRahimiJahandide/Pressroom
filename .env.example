# Database
DATABASE_URL="postgresql://user:password@localhost:5432/ai_blog?schema=public"

# AI Providers (pick one or both)
ANTHROPIC_API_KEY="sk-ant-..."
OPENAI_API_KEY="sk-..."

# Auth (dev stub uses this cookie name)
# In production, use a real auth provider